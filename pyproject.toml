[project]
name = "agentcompose"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pydantic-ai-slim[anthropic,openai]>=1.80.0",
    "pytest>=8",
    "python-dotenv>=1.2.2",
]
